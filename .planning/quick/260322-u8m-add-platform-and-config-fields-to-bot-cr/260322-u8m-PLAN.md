---
phase: quick
plan: 260322-u8m
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/pages/Dashboard.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Bot creation form includes a platform dropdown with polymarket and kalshi options"
    - "Bot creation form includes a JSON config textarea for strategy-specific settings"
    - "Platform selection is sent as config.platform in the createBot API call"
    - "Config JSON is parsed and merged into the config object sent to the API"
    - "Invalid JSON in config textarea shows a validation error and prevents submission"
    - "Strategies that do not need a platform (cross-arb) can leave it unset"
  artifacts:
    - path: "src/ui/pages/Dashboard.tsx"
      provides: "Enhanced bot creation form with platform and config fields"
  key_links:
    - from: "src/ui/pages/Dashboard.tsx"
      to: "api.createBot"
      via: "useCreateBot mutation with config object"
      pattern: "createBot\\.mutate.*config"
---

<objective>
Add platform selector and JSON config fields to the bot creation form in Dashboard.tsx.

Purpose: Currently the form only captures bot type and name. The API already accepts `config?: Record<string, unknown>` but the UI never sends it. Strategies require a `platform` field ("polymarket" | "kalshi") and strategy-specific config (e.g., minSpread, maxPositionSize, traderIds). This change lets users configure bots properly from the dashboard.

Output: Enhanced Dashboard.tsx with platform dropdown and config JSON textarea in the create form.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/ui/pages/Dashboard.tsx
@src/ui/hooks/useBots.ts
@src/ui/lib/api.ts

<interfaces>
<!-- api.createBot already accepts config -->
From src/ui/lib/api.ts:
```typescript
createBot: (data: { botType: string; name: string; config?: Record<string, unknown> }) =>
  request<any>("/bots", { method: "POST", body: JSON.stringify(data) }),
```

From src/worker/api/routes/bots.ts (POST /):
```typescript
const body = await c.req.json<{
  botType: string;
  name: string;
  config?: Record<string, unknown>;
}>();
// config is stored directly: config: body.config ?? {}
```

From src/worker/bots/base.ts:
```typescript
export interface BotConfig {
  botType: string;
  name: string;
  tickIntervalMs: number;
  dbBotId?: number;
  [key: string]: unknown;  // strategy-specific fields go here
}
```

Strategy config patterns (all extend BotConfig):
- Most strategies have `platform: "polymarket" | "kalshi"` field
- cross-arb does NOT have platform (operates across both)
- Common fields: maxPositionSize, minEdge/minSpread, tickIntervalMs
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add platform and config fields to bot creation form</name>
  <files>src/ui/pages/Dashboard.tsx</files>
  <action>
Modify the Dashboard.tsx create form to add platform and config fields. Changes:

1. Add state variables:
   - `const [newBotPlatform, setNewBotPlatform] = useState<string>("polymarket")`
   - `const [newBotConfig, setNewBotConfig] = useState<string>("")` (raw JSON string)
   - `const [configError, setConfigError] = useState<string>("")` (validation error)

2. Update `handleCreate` to build the config object:
   - Start with `config: Record<string, unknown> = {}`
   - If `newBotPlatform` is not empty (i.e., not the "None" option for cross-arb), set `config.platform = newBotPlatform`
   - If `newBotConfig` is not empty, try `JSON.parse(newBotConfig)` and merge into config via spread. On parse error, set configError and return early.
   - Clear configError on success.
   - Pass `{ botType: newBotType, name: newBotName.trim(), config }` to `createBot.mutate()`
   - On success callback, also reset `setNewBotConfig("")`, `setNewBotPlatform("polymarket")`, `setConfigError("")`

3. Add platform select below the existing bot type select, inside the create form's `div.space-y-3`:
   - Restructure the form to use a vertical layout (change the single `flex gap-3` row into multiple rows)
   - First row: bot type select + bot name input + create button (existing, keep as-is)
   - Second row: platform select with options: "polymarket", "kalshi", and "none" (for cross-arb which operates on both). Use a label "Platform" as a small text label above or inline.
   - The platform select should use the same styling as the bot type select: `className="flex-1 rounded-md border bg-background px-3 py-2 text-sm"`

4. Add config textarea below platform select:
   - Use `<textarea>` with placeholder text: `{"tickIntervalMs": 60000, "maxPositionSize": 100}` showing a realistic example
   - Style: `className="w-full rounded-md border bg-background px-3 py-2 text-sm font-mono"` with `rows={3}`
   - Label "Config (JSON)" as a small `text-xs text-muted-foreground` above it
   - Bind to `newBotConfig` / `setNewBotConfig`

5. Show configError if non-empty:
   - Below the textarea, render `{configError && <p className="text-xs text-red-500">{configError}</p>}`

6. Disable Create button additionally when configError is non-empty.

Keep all existing functionality (StatusCard, BotCard, bot list) unchanged. Follow existing code style: named function exports, cn() for class merging, inline props typing, 2-space indent, double quotes.
  </action>
  <verify>
    <automated>cd /Users/youanden/Work/trade-bot && npx tsc --noEmit --project tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <done>
    - Bot creation form shows platform dropdown with "polymarket", "kalshi", "none" options
    - Bot creation form shows a JSON config textarea with placeholder example
    - Invalid JSON shows red error text and prevents form submission
    - Valid config JSON is parsed and merged with platform into config object sent to API
    - Form fields reset on successful creation
    - TypeScript compiles without errors
  </done>
</task>

</tasks>

<verification>
- TypeScript compiles: `npx tsc --noEmit`
- Visual check: Run `cd /Users/youanden/Work/trade-bot && npx vite dev` and visit http://localhost:5173, click "New Bot", verify platform dropdown and config textarea appear
</verification>

<success_criteria>
- Dashboard create form includes platform selector and config JSON textarea
- Config is correctly sent to the API as part of the createBot payload
- JSON validation prevents submission of malformed config
- All existing dashboard functionality (bot list, start/stop, status cards) unchanged
</success_criteria>

<output>
After completion, create `.planning/quick/260322-u8m-add-platform-and-config-fields-to-bot-cr/260322-u8m-SUMMARY.md`
</output>
