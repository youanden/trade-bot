---
phase: quick
plan: 260322-wb2
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/core/exchanges/kalshi/client.ts
autonomous: true
requirements: []
must_haves:
  truths:
    - "KalshiClient.getMarkets({ status: 'active' }) sends status=open to the Kalshi API"
    - "Other status values (closed, settled) pass through unchanged"
    - "No callers need modification — mapping is internal to the client"
  artifacts:
    - path: "src/worker/core/exchanges/kalshi/client.ts"
      provides: "Status filter mapping in getMarkets"
      contains: "open"
  key_links: []
---

<objective>
Fix KalshiClient.getMarkets to map status "active" to "open" before building query params.

Purpose: The Kalshi API rejects `status=active` with HTTP 400. Kalshi uses "open" for the query param but returns "active" in response objects. The client must translate between our internal "active" status and Kalshi's "open" API param.

Output: Patched `KalshiClient.getMarkets` that maps "active" -> "open" in the query param.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/worker/core/exchanges/kalshi/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Map "active" status to "open" in KalshiClient.getMarkets query params</name>
  <files>src/worker/core/exchanges/kalshi/client.ts</files>
  <action>
In `KalshiClient.getMarkets` (line 34-52), add a status mapping before the `searchParams.set("status", ...)` call on line 42.

Replace:
```typescript
if (params?.status) searchParams.set("status", params.status);
```

With:
```typescript
if (params?.status) {
  // Kalshi API uses "open" for active markets, but response objects use "active"
  const apiStatus = params.status === "active" ? "open" : params.status;
  searchParams.set("status", apiStatus);
}
```

This keeps the mapping at the client boundary — callers (like market-maker strategy) continue passing `status: "active"` which matches our internal MarketInfo status values. The client translates to Kalshi's API vocabulary.
  </action>
  <verify>
    <automated>cd /Users/youanden/Work/trade-bot && grep -A3 'params?.status' src/worker/core/exchanges/kalshi/client.ts | grep -q 'open'</automated>
  </verify>
  <done>KalshiClient.getMarkets maps "active" to "open" before setting the query param. Other status values pass through unchanged.</done>
</task>

</tasks>

<verification>
- `grep -n 'active.*open\|open.*active' src/worker/core/exchanges/kalshi/client.ts` shows the mapping line
- The toMarketInfo helper (line 276) already maps Kalshi's response "active" status correctly — no changes needed there
- market-maker strategy (src/worker/bots/market-maker/strategy.ts line 53) continues passing `status: "active"` unchanged
</verification>

<success_criteria>
- KalshiClient.getMarkets sends `status=open` to the API when caller passes `status: "active"`
- No other files modified — fix is contained in the client
</success_criteria>

<output>
After completion, create `.planning/quick/260322-wb2-fix-kalshi-getmarkets-status-filter-map-/260322-wb2-SUMMARY.md`
</output>
