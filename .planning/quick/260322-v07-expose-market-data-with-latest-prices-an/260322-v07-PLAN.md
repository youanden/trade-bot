---
phase: quick
plan: 260322-v07
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/api/routes/markets.ts
  - src/ui/lib/api.ts
  - src/ui/hooks/useMarkets.ts
  - src/ui/pages/Markets.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "GET /markets returns market objects with yesPrice, noPrice, volume from latest price row"
    - "POST /markets/sync fetches markets from available exchanges and returns synced count"
    - "Markets page shows YES/NO prices when price data exists"
    - "Markets page auto-refreshes every 30 seconds"
    - "Markets page has a Sync button that triggers market sync"
  artifacts:
    - path: "src/worker/api/routes/markets.ts"
      provides: "Price-joined GET /markets and POST /sync endpoint"
    - path: "src/ui/pages/Markets.tsx"
      provides: "Sync button and price display"
  key_links:
    - from: "src/worker/api/routes/markets.ts"
      to: "prices table"
      via: "drizzle select from prices, group in JS"
      pattern: "db\\.select\\(\\)\\.from\\(prices\\)"
    - from: "src/ui/pages/Markets.tsx"
      to: "/api/markets/sync"
      via: "useMutation + api.syncMarkets()"
      pattern: "api\\.syncMarkets"
---

<objective>
Fix the markets API to return price data alongside market records, add a sync endpoint to trigger market fetching from exchanges, and update the UI with polling and a sync button.

Purpose: Markets page currently shows no prices because the GET endpoint only queries the markets table (prices live in a separate table). The sync endpoint enables on-demand market discovery.
Output: Working markets page with live prices and manual sync capability.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/worker/api/routes/markets.ts
@src/worker/core/db/schema.ts
@src/worker/core/market/resolver.ts
@src/worker/core/exchanges/factory.ts
@src/ui/lib/api.ts
@src/ui/hooks/useMarkets.ts
@src/ui/pages/Markets.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Join prices in GET /markets and add POST /sync endpoint</name>
  <files>src/worker/api/routes/markets.ts</files>
  <action>
1. **Route ordering** — Register `POST /sync` BEFORE `GET /:id` so Hono does not match "sync" as an :id param.

2. **Price join in GET /** — After fetching markets, query all prices from the `prices` table (`db.select().from(prices)`). Group prices by `marketId` in JS, finding the latest row per market by comparing `timestamp` strings (ISO-8601 lexicographic sort). Merge onto each market object as `yesPrice`, `noPrice`, `volume` from the latest price row. If no price row exists for a market, set these to `null`. Return the enriched array.

   Import `prices` from `../../core/db/schema`.

3. **POST /sync endpoint** — Create `app.post("/sync", ...)`:
   - Get `db` via `createDb(c.env.DB)`.
   - Parse optional `limit` from request body (default 100).
   - Build an array of `ExchangeClient[]` by trying `createExchangeClient(c.env, platform)` for each platform (`"polymarket"`, `"kalshi"`). Wrap each in try/catch — skip platforms where credentials are missing (the factory throws on missing creds).
   - If no clients available, return `c.json({ synced: 0, error: "No exchange credentials configured" }, 200)`.
   - Create `new MarketResolver(db, clients)` and call `await resolver.syncMarkets(limit)`.
   - Return `c.json({ synced: count })`.
   - Import `createExchangeClient` from `../../core/exchanges/factory`.
  </action>
  <verify>
    <automated>cd /Users/youanden/Work/trade-bot && npx wrangler dev --test-scheduled 2>&1 | head -5 || echo "Wrangler check skipped — verify no TS errors with: npx tsc --noEmit --pretty 2>&1 | grep -E 'markets\.(ts|error)' | head -20"</automated>
  </verify>
  <done>GET /markets returns objects with yesPrice/noPrice/volume fields. POST /markets/sync creates exchange clients (skipping missing creds) and returns { synced: N }. POST /sync is registered before GET /:id.</done>
</task>

<task type="auto">
  <name>Task 2: Add syncMarkets to API client, polling to hook, and sync button to UI</name>
  <files>src/ui/lib/api.ts, src/ui/hooks/useMarkets.ts, src/ui/pages/Markets.tsx</files>
  <action>
1. **api.ts** — Add `syncMarkets` method to the `api` object:
   ```
   syncMarkets: () => request<{ synced: number }>("/markets/sync", { method: "POST" }),
   ```

2. **useMarkets.ts** — Add `refetchInterval: 30_000` to the `useQuery` options object so markets auto-refresh every 30 seconds.

3. **Markets.tsx** — Add a sync button and useMutation:
   - Import `useMutation` and `useQueryClient` from `@tanstack/react-query`.
   - Import `api` from `../lib/api`.
   - Inside the `Markets` component, create:
     ```
     const qc = useQueryClient();
     const sync = useMutation({
       mutationFn: () => api.syncMarkets(),
       onSuccess: () => qc.invalidateQueries({ queryKey: ["markets"] }),
     });
     ```
   - Add a "Sync Markets" button next to the search input in the header flex container. Style with Tailwind: `rounded-md bg-primary px-3 py-2 text-sm text-primary-foreground hover:bg-primary/90 disabled:opacity-50`. Set `disabled={sync.isPending}`. Show "Syncing..." text when `sync.isPending`, otherwise "Sync Markets". `onClick={() => sync.mutate()}`.
   - The existing price display (`m.yes_price`, `m.no_price`) should be updated to use `m.yesPrice` and `m.noPrice` (camelCase, matching the enriched API response). Also display `m.volume` if present — add a small volume indicator below the price row: `{m.volume != null && <span className="text-xs text-muted-foreground">Vol: {m.volume.toLocaleString()}</span>}`.
  </action>
  <verify>
    <automated>cd /Users/youanden/Work/trade-bot && npx tsc --noEmit --pretty 2>&1 | grep -c 'error' || echo "0 errors"</automated>
  </verify>
  <done>api.syncMarkets() exists. useMarkets polls every 30s. Markets page has a working Sync button with loading state. Price fields reference yesPrice/noPrice (camelCase). Volume displayed when available.</done>
</task>

</tasks>

<verification>
- `GET /api/markets` response objects include `yesPrice`, `noPrice`, `volume` fields
- `POST /api/markets/sync` returns `{ synced: N }` (or `{ synced: 0, error: ... }` when no creds)
- Markets.tsx renders prices using camelCase field names
- useMarkets has `refetchInterval: 30_000`
- Sync button shows loading state during mutation and invalidates cache on success
</verification>

<success_criteria>
Markets page displays YES/NO prices from the prices table, auto-refreshes every 30 seconds, and provides a manual sync button for on-demand market discovery.
</success_criteria>

<output>
After completion, create `.planning/quick/260322-v07-expose-market-data-with-latest-prices-an/260322-v07-SUMMARY.md`
</output>
