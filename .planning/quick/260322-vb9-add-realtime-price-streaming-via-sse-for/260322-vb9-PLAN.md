---
phase: quick
plan: 260322-vb9
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/api/routes/markets.ts
  - src/ui/hooks/useMarketStream.ts
  - src/ui/pages/Markets.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - "Markets page shows live-updating YES/NO prices without manual refresh"
    - "SSE stream sends initial snapshot of all active market prices on connect"
    - "Price ticks arrive every ~2 seconds with random-walk simulation"
    - "Stream auto-closes after 10 minutes to prevent zombie connections"
  artifacts:
    - path: "src/worker/api/routes/markets.ts"
      provides: "GET /markets/stream SSE endpoint"
      contains: "text/event-stream"
    - path: "src/ui/hooks/useMarketStream.ts"
      provides: "EventSource hook returning price overrides map"
      exports: ["useMarketStream", "PriceOverride"]
    - path: "src/ui/pages/Markets.tsx"
      provides: "Market cards with SSE price overrides merged in"
  key_links:
    - from: "src/ui/hooks/useMarketStream.ts"
      to: "/api/markets/stream"
      via: "EventSource connection"
      pattern: "new EventSource.*markets/stream"
    - from: "src/ui/pages/Markets.tsx"
      to: "src/ui/hooks/useMarketStream.ts"
      via: "useMarketStream() hook call"
      pattern: "useMarketStream"
---

<objective>
Add realtime price streaming to the Markets page via Server-Sent Events.

Purpose: Replace polling-only price updates with a live SSE stream that simulates price ticks (random walk) for seeded dev data, giving the dashboard a realtime feel.
Output: SSE endpoint at GET /markets/stream, useMarketStream hook, Markets.tsx wired to overlay SSE prices.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/worker/api/routes/markets.ts
@src/worker/core/db/schema.ts
@src/ui/hooks/useMarkets.ts
@src/ui/pages/Markets.tsx
@src/ui/hooks/useMarketStream.ts (new file)

<interfaces>
<!-- From src/worker/core/db/schema.ts -->
markets table: id (integer PK), platform (text), platformId (text), title (text), status (text), category (text), endDate (text), ...
prices table: id (integer PK), marketId (integer FK->markets.id), yesPrice (real), noPrice (real), yesBid (real), yesAsk (real), volume (real), timestamp (text)

<!-- From src/worker/api/routes/markets.ts -->
Hono app bound to Env. Routes: GET /, POST /sync, POST /seed, GET /:id, GET /matches, GET /:id/linked, POST /links
GET / enriches markets with latest price (yesPrice, noPrice, volume) from prices table.

<!-- From src/ui/hooks/useMarkets.ts -->
export function useMarkets(limit?: number) — useQuery with queryKey ["markets", limit], refetchInterval 30_000

<!-- From src/ui/pages/Markets.tsx -->
Uses useMarkets(200), renders filtered.map((m: any) => ...) cards showing m.yesPrice, m.noPrice, m.volume
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GET /markets/stream SSE endpoint</name>
  <files>src/worker/api/routes/markets.ts</files>
  <action>
Add a new route `app.get("/stream", ...)` BEFORE the `app.get("/:id", ...)` route (line 144) to avoid param collision.

Implementation:
1. Query active markets from DB: `db.select().from(markets).where(eq(markets.status, "active")).limit(50)`
2. Query all prices, build a `latestByMarket` Map keyed by marketId holding the row with the latest timestamp (compare timestamps as strings — ISO-8601 lexicographic ordering works)
3. Build a `priceState` Map of marketId -> current yesPrice (default 0.5 if no price row)
4. Create a `ReadableStream` with:
   - `start(controller)`:
     - Helper `send(event, data)` that enqueues `event: ${event}\ndata: ${JSON.stringify(data)}\n\n` via TextEncoder
     - Send `"snapshot"` event with array of `{ id, yesPrice, noPrice }` for all active markets
     - `setInterval` every 2000ms: pick random market, apply random walk delta `(Math.random() - 0.5) * 0.04`, clamp to [0.01, 0.99], update priceState, send `"tick"` event with `{ id, yesPrice, noPrice: +(1 - yesPrice).toFixed(4) }`
     - `setTimeout` at 600_000ms (10 min): clearInterval + controller.close()
5. Return `new Response(stream, { headers: { "Content-Type": "text/event-stream", "Cache-Control": "no-cache", "Connection": "keep-alive", "X-Accel-Buffering": "no" } })` — do NOT use `c.json()`.

Import `eq` from drizzle-orm (already imported on line 4).
  </action>
  <verify>
    <automated>cd /Users/youanden/Work/trade-bot && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>GET /api/markets/stream returns Content-Type text/event-stream, sends snapshot event on connect, sends tick events every 2s with random-walk prices, auto-closes after 10 minutes.</done>
</task>

<task type="auto">
  <name>Task 2: Create useMarketStream hook and wire into Markets.tsx</name>
  <files>src/ui/hooks/useMarketStream.ts, src/ui/pages/Markets.tsx</files>
  <action>
**useMarketStream.ts** (new file):
1. Export type `PriceOverride = { yesPrice: number; noPrice: number }`
2. Export function `useMarketStream()` returning `Map<number, PriceOverride>`
3. Use `useState<Map<number, PriceOverride>>` initialized to empty Map
4. Use `useRef<EventSource | null>` to hold the connection
5. In `useEffect([], ...)`:
   - Create `new EventSource("/api/markets/stream")`
   - Listen for `"snapshot"` event: parse JSON array of `{ id, yesPrice, noPrice }`, build new Map from it, call setOverrides
   - Listen for `"tick"` event: parse JSON `{ id, yesPrice, noPrice }`, clone Map via `new Map(prev)`, set the entry, call setOverrides
   - On `es.onerror`: call `es.close()`
   - Cleanup: call `es.close()`
6. Return the overrides Map

**Markets.tsx** changes:
1. Import `useMarketStream` from `"../hooks/useMarketStream"`
2. Call `const priceOverrides = useMarketStream();` inside the Markets component (after existing hooks)
3. In the market card rendering (inside `filtered.map`), override prices from SSE:
   - Before the card JSX, compute: `const yesPrice = priceOverrides.get(m.id)?.yesPrice ?? m.yesPrice;`
   - And: `const noPrice = priceOverrides.get(m.id)?.noPrice ?? m.noPrice;`
   - Replace `m.yesPrice` with `yesPrice` and `m.noPrice` with `noPrice` in the JSX (lines 94-103)
4. In `useMarkets` hook call, keep `refetchInterval: 30_000` as fallback for initial data load (SSE handles live updates)
  </action>
  <verify>
    <automated>cd /Users/youanden/Work/trade-bot && npx tsc --noEmit --pretty 2>&1 | head -30</automated>
  </verify>
  <done>Markets page connects to SSE stream on mount, displays snapshot prices immediately, updates individual market card prices in realtime as tick events arrive, and cleans up EventSource on unmount.</done>
</task>

</tasks>

<verification>
1. `npx tsc --noEmit` passes with no errors
2. Start dev servers (`wrangler dev` + `vite dev`), seed markets, navigate to Markets page
3. Observe market cards update prices every ~2 seconds without page refresh
4. Check browser DevTools Network tab shows `/api/markets/stream` as EventStream with snapshot + tick events
</verification>

<success_criteria>
- GET /api/markets/stream returns SSE with snapshot and tick events
- useMarketStream hook connects, parses events, returns live price overrides
- Markets.tsx renders SSE prices over static DB prices
- TypeScript compiles cleanly
</success_criteria>

<output>
After completion, create `.planning/quick/260322-vb9-add-realtime-price-streaming-via-sse-for/260322-vb9-SUMMARY.md`
</output>
