---
phase: 03-exchange-simulation
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/worker/core/exchanges/factory.ts
  - test/core/sim-client.test.ts
autonomous: true
requirements:
  - EXCH-07

must_haves:
  truths:
    - "createExchangeClient with a simulationFeed parameter returns a SimExchangeClient instance"
    - "createExchangeClient without simulationFeed still returns real PolymarketClient or KalshiClient (no regression)"
    - "The factory signature change is a single optional third parameter — existing callers need zero changes"
  artifacts:
    - path: "src/worker/core/exchanges/factory.ts"
      provides: "Extended factory with simulation mode"
      exports: ["createExchangeClient"]
      contains: "simulationFeed"
    - path: "test/core/sim-client.test.ts"
      provides: "EXCH-07 factory extension test"
      contains: "EXCH-07"
  key_links:
    - from: "src/worker/core/exchanges/factory.ts"
      to: "src/worker/core/simulation/sim-client.ts"
      via: "import SimExchangeClient"
      pattern: "SimExchangeClient"
    - from: "src/worker/core/exchanges/factory.ts"
      to: "src/worker/core/simulation/feed.ts"
      via: "import type PriceFeed"
      pattern: "PriceFeed"
---

<objective>
Extend the existing `createExchangeClient` factory with an optional third parameter that returns a `SimExchangeClient` when a simulation feed is provided. This is the one-line production change that enables simulation mode.

Purpose: Strategies call `createExchangeClient(env, platform)` unchanged. The backtest engine (Phase 4) will pass the third `simFeed` argument to swap in the simulated client. No strategy code changes needed.

Output: Modified `src/worker/core/exchanges/factory.ts` with optional `simFeed` parameter, plus EXCH-07 test in `test/core/sim-client.test.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-exchange-simulation/03-RESEARCH.md
@.planning/phases/03-exchange-simulation/03-01-SUMMARY.md

<interfaces>
<!-- Current factory signature to extend -->

From src/worker/core/exchanges/factory.ts:
```typescript
export function createExchangeClient(
  env: Env,
  platform: "polymarket" | "kalshi"
): ExchangeClient {
  // ... existing logic
}
```

<!-- SimExchangeClient from Plan 01 -->

From src/worker/core/simulation/sim-client.ts (created in Plan 01):
```typescript
export interface SimClientConfig {
  platform: "polymarket" | "kalshi";
  feed: PriceFeed;
  simulatedNow: () => string;
  virtualBalance: number;
  takerFeeRate?: number;
  partialFillRate?: number;
  leg2FailRate?: number;
  seed?: number;
}

export class SimExchangeClient implements ExchangeClient {
  constructor(config: SimClientConfig);
  // ... all 11 ExchangeClient methods
}
```

From src/worker/core/simulation/feed.ts:
```typescript
export class PriceFeed {
  constructor(scenario: GeneratedScenario);
  latestAt(simulatedNow: string): GeneratedScenario["prices"][number] | undefined;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend factory with simulation mode and add EXCH-07 test</name>
  <files>src/worker/core/exchanges/factory.ts, test/core/sim-client.test.ts</files>
  <read_first>
    - src/worker/core/exchanges/factory.ts (current factory — see exact signature, imports, and existing logic)
    - src/worker/core/simulation/sim-client.ts (SimExchangeClient and SimClientConfig exports from Plan 01)
    - src/worker/core/simulation/feed.ts (PriceFeed class)
    - test/core/sim-client.test.ts (existing tests from Plan 01 — append EXCH-07 test)
  </read_first>
  <action>
    1. Modify `src/worker/core/exchanges/factory.ts`:
       - Add imports at top (using `import type` for type-only imports per project convention):
         ```typescript
         import type { PriceFeed } from "../simulation/feed";
         import type { SimClientConfig } from "../simulation/sim-client";
         import { SimExchangeClient } from "../simulation/sim-client";
         ```
       - Change `createExchangeClient` signature to add optional third parameter:
         ```typescript
         export function createExchangeClient(
           env: Env,
           platform: "polymarket" | "kalshi",
           simFeed?: { feed: PriceFeed; config?: Partial<Omit<SimClientConfig, "platform" | "feed">> }
         ): ExchangeClient {
         ```
       - Add simulation branch as first check in function body (before existing platform logic):
         ```typescript
         if (simFeed) {
           return new SimExchangeClient({
             platform,
             feed: simFeed.feed,
             simulatedNow: simFeed.config?.simulatedNow ?? (() => new Date().toISOString()),
             virtualBalance: simFeed.config?.virtualBalance ?? 1000,
             ...simFeed.config,
           });
         }
         ```
       - All existing logic below the new `if (simFeed)` block stays EXACTLY as-is

    2. Append EXCH-07 test to `test/core/sim-client.test.ts`:
       - Add a new describe block at the end:
         ```typescript
         describe("EXCH-07 factory extension", () => {
           test("createExchangeClient with simFeed returns SimExchangeClient", async () => {
             const scenario = generateScenario({ type: "flat", seed: 42, ticks: 10 });
             const feed = new PriceFeed(scenario);
             const client = createExchangeClient(
               {} as Env,
               "polymarket",
               { feed, config: { simulatedNow: () => scenario.prices[5].timestamp, virtualBalance: 500 } }
             );
             expect(client).toBeInstanceOf(SimExchangeClient);
             expect(client.platform).toBe("polymarket");
             const balance = await client.getBalance();
             expect(balance).toBe(500);
           });

           test("createExchangeClient without simFeed throws on missing credentials (existing behavior)", () => {
             expect(() => createExchangeClient({} as Env, "polymarket")).toThrow("Missing POLYMARKET_PRIVATE_KEY");
             expect(() => createExchangeClient({} as Env, "kalshi")).toThrow("Missing KALSHI_API_KEY");
           });
         });
         ```
       - Add imports at top of test file if not present: `import { createExchangeClient } from "../../src/worker/core/exchanges/factory";`

    3. Run `bun test test/core/sim-client.test.ts` — all tests including EXCH-07 must pass
    4. Run `bun test` — full suite must pass (no regressions in existing strategy tests that use createExchangeClient)
  </action>
  <verify>
    <automated>bun test test/core/sim-client.test.ts && bun test</automated>
  </verify>
  <acceptance_criteria>
    - src/worker/core/exchanges/factory.ts contains `simFeed?:` in the createExchangeClient signature
    - src/worker/core/exchanges/factory.ts contains `if (simFeed)` branch
    - src/worker/core/exchanges/factory.ts contains `new SimExchangeClient(`
    - src/worker/core/exchanges/factory.ts contains `import { SimExchangeClient }` from simulation/sim-client
    - src/worker/core/exchanges/factory.ts contains `import type { PriceFeed }` from simulation/feed
    - test/core/sim-client.test.ts contains `describe("EXCH-07`
    - test/core/sim-client.test.ts contains `createExchangeClient(`
    - test/core/sim-client.test.ts contains `toBeInstanceOf(SimExchangeClient)`
    - `bun test test/core/sim-client.test.ts` exits 0
    - `bun test` exits 0 (full suite green — no regressions)
  </acceptance_criteria>
  <done>createExchangeClient accepts optional simFeed parameter. When provided, returns SimExchangeClient instance with configured balance. Without it, existing behavior unchanged (throws on missing credentials). EXCH-07 test passes.</done>
</task>

</tasks>

<verification>
- `bun test test/core/sim-client.test.ts` — EXCH-07 test passes alongside EXCH-01..06
- `bun test` — full suite green (existing strategy tests unaffected)
- `grep "simFeed" src/worker/core/exchanges/factory.ts` returns matches
</verification>

<success_criteria>
- Factory extended with one optional parameter — existing callers unaffected
- SimExchangeClient returned when simFeed provided
- All tests green including EXCH-07
</success_criteria>

<output>
After completion, create `.planning/phases/03-exchange-simulation/03-02-SUMMARY.md`
</output>
