---
phase: 09-add-discord-trade-notifications-and-leaderboard-copy-strategy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/worker/core/notifications/discord.ts
  - env.d.ts
  - test/core/discord.test.ts
autonomous: true
requirements:
  - DISC-01
  - DISC-02
  - DISC-03

must_haves:
  truths:
    - "notifyDiscord builds a Discord embed payload with trade type emoji, market name, outcome, price, shares, cost, fee, P&L, trader address, and portfolio summary footer"
    - "notifyDiscord POSTs the embed to the provided webhook URL via native fetch"
    - "notifyDiscord silently swallows errors without throwing"
    - "The Env interface includes an optional DISCORD_WEBHOOK_URL binding"
  artifacts:
    - path: "src/worker/core/notifications/discord.ts"
      provides: "Discord notification utility with TradeNotification type and notifyDiscord function"
      exports: ["notifyDiscord", "TradeNotification", "TradeType"]
    - path: "env.d.ts"
      provides: "DISCORD_WEBHOOK_URL optional binding on Env interface"
      contains: "DISCORD_WEBHOOK_URL"
    - path: "test/core/discord.test.ts"
      provides: "Unit tests for Discord notification service"
      min_lines: 50
  key_links:
    - from: "src/worker/core/notifications/discord.ts"
      to: "Discord webhook URL"
      via: "native fetch POST"
      pattern: "fetch\\(webhookUrl"
---

<objective>
Create the Discord notification service and add the DISCORD_WEBHOOK_URL env binding.

Purpose: Standalone utility that formats trade events into Discord embeds and POSTs them via native fetch (per D-01, D-02). This is consumed by the copy-trader strategy integration in Plan 03.
Output: `src/worker/core/notifications/discord.ts`, updated `env.d.ts`, and `test/core/discord.test.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-add-discord-trade-notifications-and-leaderboard-copy-strategy/09-CONTEXT.md
@.planning/phases/09-add-discord-trade-notifications-and-leaderboard-copy-strategy/09-RESEARCH.md

<interfaces>
<!-- Env interface from env.d.ts (will be extended) -->
From env.d.ts:
```typescript
interface Env {
  DB: D1Database;
  BOT_DO: DurableObjectNamespace;
  ASSETS: Fetcher;
  ENVIRONMENT: string;
  POLYMARKET_API_KEY?: string;
  POLYMARKET_PRIVATE_KEY?: string;
  POLYMARKET_API_SECRET?: string;
  POLYMARKET_PASSPHRASE?: string;
  POLYMARKET_ADDRESS?: string;
  KALSHI_API_KEY?: string;
  KALSHI_API_SECRET?: string;
  AUTH_TOKEN?: string;
  AI?: Ai;
}
```

From src/worker/bots/base.ts:
```typescript
export interface TradeRecord {
  marketId: number;
  platform: string;
  side: "buy" | "sell";
  outcome: "yes" | "no";
  price: number;
  size: number;
  reason?: string;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Discord notification service and unit tests</name>
  <files>src/worker/core/notifications/discord.ts, test/core/discord.test.ts</files>
  <read_first>
    - env.d.ts
    - src/worker/core/utils/logger.ts
    - test/helpers/mocks.ts
    - .planning/phases/09-add-discord-trade-notifications-and-leaderboard-copy-strategy/09-RESEARCH.md
  </read_first>
  <behavior>
    - Test 1: notifyDiscord sends POST to webhook URL with Content-Type application/json
    - Test 2: Embed title contains trade type emoji and label (e.g., "COPY BUY" with emoji)
    - Test 3: Embed fields include Market, Outcome, Price, Shares, Cost, Fee (all required by DISC-03)
    - Test 4: P&L field present when pnl is provided, absent when undefined
    - Test 5: Copied Trader field shows abbreviated address (0x1234...abcd format) when traderAddress provided
    - Test 6: Footer text contains cash, equity, realized P&L, fees, net, positions, trades from portfolioSummary
    - Test 7: notifyDiscord does NOT throw when fetch returns non-ok status (e.g., 429)
    - Test 8: notifyDiscord does NOT throw when fetch rejects with network error
    - Test 9: notifyDiscord is not called when webhookUrl is empty string (guard at call site)
  </behavior>
  <action>
    Create `src/worker/core/notifications/discord.ts` with:

    1. Export `TradeType` as string union: `"COPY_BUY" | "COPY_SELL" | "TAKE_PROFIT" | "STOP_LOSS"` (per D-01, using native fetch only per D-02)

    2. Export `TradeNotification` interface with fields:
       - `tradeType: TradeType`
       - `marketName: string`
       - `outcome: "yes" | "no"`
       - `price: number`
       - `shares: number`
       - `cost: number`
       - `fee: number`
       - `pnl?: number` (sells only)
       - `traderAddress?: string` (copy trades)
       - `category?: string`
       - `timestamp: string` (ISO-8601)
       - `portfolioSummary: { cash: number; equity: number; realizedPnl: number; totalFees: number; netPnl: number; openPositions: number; totalTrades: number }`

    3. Module-level constants:
       - `TRADE_EMOJI: Record<TradeType, string>` = `{ COPY_BUY: "📈", COPY_SELL: "📉", TAKE_PROFIT: "✅", STOP_LOSS: "🛑" }`
       - `TRADE_COLOR: Record<TradeType, number>` = `{ COPY_BUY: 3066993, COPY_SELL: 10038562, TAKE_PROFIT: 3066993, STOP_LOSS: 15158332 }`

    4. Export async function `notifyDiscord(webhookUrl: string, notification: TradeNotification): Promise<void>`:
       - Build embed fields array: Market (inline: false), Outcome (uppercase, inline: true), Price (`$X.XXX`, inline: true), Shares (X.XX, inline: true), Cost (`$X.XX`, inline: true), Fee (`$X.XX`, inline: true)
       - Conditionally add P&L field if `pnl !== undefined`
       - Conditionally add Copied Trader field if `traderAddress` is truthy, abbreviated as `${addr.slice(0,6)}...${addr.slice(-4)}`
       - Conditionally add Category field if `category` is truthy, formatted as `[category]`
       - Build footer text: `Cash: $X.XX | Equity: $X.XX | Realized P&L: $X.XX | Fees: $X.XX | Net: $X.XX | Positions: N | Trades: N`
       - Slice all field values to max 1024 chars, footer to max 2048 chars (Discord limits)
       - POST to webhookUrl with `{ embeds: [{ title, color, fields, footer: { text }, timestamp }] }`
       - Wrap entire fetch in try/catch — log warning on non-ok response (`console.warn(\`discord-webhook:${res.status}\`)`), silently catch network errors
       - NEVER throw — fire-and-forget pattern

    Create `test/core/discord.test.ts` using bun:test:
    - Override `globalThis.fetch` in beforeEach to capture calls, restore in afterEach
    - Captured fetch mock returns `new Response(null, { status: 204 })` by default
    - Build a complete `TradeNotification` fixture for reuse across tests
    - For error tests: mock fetch to return 429 or throw TypeError("Network error")
  </action>
  <verify>
    <automated>cd /Users/youanden/Work/trade-bot && bun test test/core/discord.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/worker/core/notifications/discord.ts contains `export type TradeType =`
    - src/worker/core/notifications/discord.ts contains `export interface TradeNotification`
    - src/worker/core/notifications/discord.ts contains `export async function notifyDiscord(`
    - src/worker/core/notifications/discord.ts contains `fetch(webhookUrl`
    - src/worker/core/notifications/discord.ts contains `"Content-Type": "application/json"`
    - src/worker/core/notifications/discord.ts does NOT contain `import` of any discord library
    - src/worker/core/notifications/discord.ts contains `try {` and `catch` (fire-and-forget)
    - test/core/discord.test.ts contains at least 7 test cases
    - `bun test test/core/discord.test.ts` exits 0
  </acceptance_criteria>
  <done>All 9 behavior tests pass. notifyDiscord builds correct Discord embed payloads, POSTs via native fetch, and silently handles errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add DISCORD_WEBHOOK_URL to Env interface</name>
  <files>env.d.ts</files>
  <read_first>
    - env.d.ts
  </read_first>
  <action>
    Add `DISCORD_WEBHOOK_URL?: string;` to the `Env` interface in `env.d.ts`, placed after the `AUTH_TOKEN?: string;` line and before `AI?: Ai;`.

    The field is optional (`?`) because:
    - Not all environments have Discord configured
    - The notification guard in strategy code checks `if (env.DISCORD_WEBHOOK_URL)` before calling notifyDiscord
    - Secrets are set via `wrangler secret put DISCORD_WEBHOOK_URL`, not wrangler.toml vars

    No wrangler.toml changes needed (secrets are not declared in config).
  </action>
  <verify>
    <automated>cd /Users/youanden/Work/trade-bot && grep -n "DISCORD_WEBHOOK_URL" env.d.ts</automated>
  </verify>
  <acceptance_criteria>
    - env.d.ts contains `DISCORD_WEBHOOK_URL?: string;`
    - env.d.ts still contains all existing Env fields (DB, BOT_DO, ASSETS, etc.)
    - The line appears inside the `interface Env { }` block
  </acceptance_criteria>
  <done>Env interface declares DISCORD_WEBHOOK_URL as optional string binding. No hardcoded webhook URLs exist in source (per DISC-02).</done>
</task>

</tasks>

<verification>
- `bun test test/core/discord.test.ts` passes all tests
- `grep "DISCORD_WEBHOOK_URL" env.d.ts` shows the binding declaration
- `grep -r "discord" src/worker/core/notifications/` confirms the notification module exists
- No external Discord library in package.json (per D-02)
</verification>

<success_criteria>
1. Discord notification utility exists at `src/worker/core/notifications/discord.ts` with exported `notifyDiscord`, `TradeNotification`, and `TradeType`
2. Unit tests verify embed payload structure, conditional fields, error swallowing
3. `env.d.ts` declares `DISCORD_WEBHOOK_URL?: string` on the `Env` interface
4. Zero new npm dependencies added (per D-02)
</success_criteria>

<output>
After completion, create `.planning/phases/09-add-discord-trade-notifications-and-leaderboard-copy-strategy/09-01-SUMMARY.md`
</output>
