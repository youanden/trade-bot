---
phase: 09-add-discord-trade-notifications-and-leaderboard-copy-strategy
plan: 03
type: execute
wave: 2
depends_on:
  - 09-01
  - 09-02
files_modified:
  - src/worker/bots/copy-trader/strategy.ts
  - test/strategies/copy-trader.test.ts
autonomous: true
requirements:
  - DISC-04
  - LEAD-02

must_haves:
  truths:
    - "When env.DISCORD_WEBHOOK_URL is set, copyTraderTick calls notifyDiscord after each successful recordTrade"
    - "When env.DISCORD_WEBHOOK_URL is absent, no notification code is called"
    - "When config.leaderboardMode is true, copyTraderTick refreshes tracked traders from leaderboard at the configured interval"
    - "Leaderboard traders are upserted to the tracked_traders DB table with normalized lowercase addresses"
    - "Leaderboard refresh is skipped when less than leaderboardRefreshMs has elapsed since _lastLeaderboardRefresh"
  artifacts:
    - path: "src/worker/bots/copy-trader/strategy.ts"
      provides: "Copy trader strategy with Discord notification and leaderboard integration"
      contains: "notifyDiscord"
    - path: "test/strategies/copy-trader.test.ts"
      provides: "Extended tests for Discord integration and leaderboard refresh"
      min_lines: 80
  key_links:
    - from: "src/worker/bots/copy-trader/strategy.ts"
      to: "src/worker/core/notifications/discord.ts"
      via: "import notifyDiscord"
      pattern: "import.*notifyDiscord.*from.*notifications/discord"
    - from: "src/worker/bots/copy-trader/strategy.ts"
      to: "src/worker/core/exchanges/polymarket/leaderboard.ts"
      via: "import fetchLeaderboard"
      pattern: "import.*fetchLeaderboard.*from.*leaderboard"
    - from: "src/worker/bots/copy-trader/strategy.ts"
      to: "tracked_traders table"
      via: "db.insert(trackedTraders) upsert"
      pattern: "trackedTraders"
---

<objective>
Wire Discord notifications and leaderboard refresh into the copy-trader strategy tick flow.

Purpose: Integrate the notification service (Plan 01) and leaderboard client (Plan 02) into `copyTraderTick`. After each successful trade, post a Discord notification if the webhook URL is configured. Before the trader loop, refresh the tracked traders list from the leaderboard if leaderboard mode is enabled and the refresh interval has elapsed.
Output: Updated `src/worker/bots/copy-trader/strategy.ts` and extended `test/strategies/copy-trader.test.ts`.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-add-discord-trade-notifications-and-leaderboard-copy-strategy/09-CONTEXT.md
@.planning/phases/09-add-discord-trade-notifications-and-leaderboard-copy-strategy/09-RESEARCH.md
@.planning/phases/09-add-discord-trade-notifications-and-leaderboard-copy-strategy/09-01-SUMMARY.md
@.planning/phases/09-add-discord-trade-notifications-and-leaderboard-copy-strategy/09-02-SUMMARY.md

<interfaces>
<!-- From Plan 01: Discord notification service -->
From src/worker/core/notifications/discord.ts:
```typescript
export type TradeType = "COPY_BUY" | "COPY_SELL" | "TAKE_PROFIT" | "STOP_LOSS";

export interface TradeNotification {
  tradeType: TradeType;
  marketName: string;
  outcome: "yes" | "no";
  price: number;
  shares: number;
  cost: number;
  fee: number;
  pnl?: number;
  traderAddress?: string;
  category?: string;
  timestamp: string;
  portfolioSummary: {
    cash: number;
    equity: number;
    realizedPnl: number;
    totalFees: number;
    netPnl: number;
    openPositions: number;
    totalTrades: number;
  };
}

export async function notifyDiscord(
  webhookUrl: string,
  notification: TradeNotification
): Promise<void>;
```

<!-- From Plan 02: Leaderboard client and config -->
From src/worker/core/exchanges/polymarket/leaderboard.ts:
```typescript
export interface LeaderboardEntry {
  rank: number;
  proxyWallet: string;
  userName: string;
  pnl: number;
  vol: number;
}

export interface LeaderboardParams {
  timePeriod?: "DAY" | "WEEK" | "MONTH" | "ALL";
  orderBy?: "PNL" | "VOL";
  limit?: number;
  offset?: number;
  category?: string;
}

export async function fetchLeaderboard(
  params?: LeaderboardParams
): Promise<LeaderboardEntry[]>;
```

From src/worker/bots/copy-trader/config.ts (after Plan 02):
```typescript
export interface CopyTraderConfig extends BotConfig {
  botType: "copy-trader";
  platform: "polymarket" | "kalshi";
  traderIds: string[];
  maxPositionSize: number;
  sizeFraction: number;
  maxSlippage: number;
  minEdge: number;
  copySells: boolean;
  leaderboardMode?: boolean;
  leaderboardRefreshMs?: number;
  leaderboardTopN?: number;
  leaderboardTimePeriod?: "DAY" | "WEEK" | "MONTH" | "ALL";
  _lastLeaderboardRefresh?: string;
}
```

<!-- Existing strategy and schema references -->
From src/worker/bots/copy-trader/strategy.ts:
```typescript
export async function copyTraderTick(bot: BaseBotDO, env: Env): Promise<void>;
// processTrader is internal, called per traderId
// After each successful recordTrade() call is the integration point for Discord
```

From src/worker/core/db/schema.ts:
```typescript
export const trackedTraders = sqliteTable("tracked_traders", {
  id: integer().primaryKey({ autoIncrement: true }),
  platform: text().notNull(),
  traderId: text("trader_id").notNull(),
  alias: text(),
  winRate: real("win_rate"),
  totalPnl: real("total_pnl"),
  isActive: integer("is_active", { mode: "boolean" }).notNull().default(true),
  createdAt: text("created_at").notNull().default(sql`(datetime('now'))`),
});
```

From test/helpers/mocks.ts:
```typescript
export function makeMockBot(config: Record<string, unknown> & { botType: string });
export function makeTestEnv(db: TestDb): Record<string, unknown>;
export class MockExchangeClient implements ExchangeClient;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate Discord notifications and leaderboard refresh into copyTraderTick</name>
  <files>src/worker/bots/copy-trader/strategy.ts</files>
  <read_first>
    - src/worker/bots/copy-trader/strategy.ts
    - src/worker/bots/copy-trader/config.ts
    - src/worker/core/notifications/discord.ts
    - src/worker/core/exchanges/polymarket/leaderboard.ts
    - src/worker/core/db/schema.ts
    - src/worker/core/db/client.ts
  </read_first>
  <action>
    Modify `src/worker/bots/copy-trader/strategy.ts` to add two integrations:

    **1. Imports (add at top):**
    ```typescript
    import { notifyDiscord } from "../../core/notifications/discord";
    import type { TradeNotification } from "../../core/notifications/discord";
    import { fetchLeaderboard } from "../../core/exchanges/polymarket/leaderboard";
    ```

    **2. Leaderboard refresh at start of copyTraderTick (LEAD-02):**

    Add a `maybeRefreshLeaderboard` helper function (not exported) after the `fetchTraderPositions` function:

    ```typescript
    async function maybeRefreshLeaderboard(
      bot: BaseBotDO,
      env: Env,
      config: CopyTraderConfig,
      db: ReturnType<typeof createDb>
    ): Promise<void> {
      const refreshMs = config.leaderboardRefreshMs ?? 3_600_000;
      const lastRefresh = config._lastLeaderboardRefresh;
      const now = Date.now();

      if (lastRefresh && now - new Date(lastRefresh).getTime() < refreshMs) {
        return; // Not due yet
      }

      let entries;
      try {
        entries = await fetchLeaderboard({
          timePeriod: config.leaderboardTimePeriod ?? "WEEK",
          orderBy: "PNL",
          limit: config.leaderboardTopN ?? 10,
        });
      } catch (err) {
        log.error("leaderboard:fetch-failed", {
          error: err instanceof Error ? err.message : String(err),
        });
        return;
      }

      const nowIso = new Date().toISOString();
      for (const entry of entries) {
        // Use select-then-insert/update pattern since tracked_traders has no unique constraint
        const existing = await db.select().from(trackedTraders)
          .where(and(
            eq(trackedTraders.platform, "polymarket"),
            eq(trackedTraders.traderId, entry.proxyWallet)
          ))
          .limit(1);

        if (existing.length > 0) {
          await db.update(trackedTraders)
            .set({ alias: entry.userName, totalPnl: entry.pnl, isActive: true })
            .where(eq(trackedTraders.id, existing[0].id));
        } else {
          await db.insert(trackedTraders).values({
            platform: "polymarket",
            traderId: entry.proxyWallet,
            alias: entry.userName,
            totalPnl: entry.pnl,
            winRate: null,
            isActive: true,
            createdAt: nowIso,
          });
        }
      }

      // Update traderIds and persist last refresh time
      const newTraderIds = entries.map((e) => e.proxyWallet);
      config.traderIds = newTraderIds;
      config._lastLeaderboardRefresh = new Date().toISOString();

      // Persist updated config via bot.updateConfig if available
      if (typeof (bot as any).updateConfig === "function") {
        await (bot as any).updateConfig(config);
      }

      log.info("leaderboard:refreshed", { traderCount: entries.length });
    }
    ```

    In `copyTraderTick`, add after the `isDailyLossBreached` check and before the exchange client creation:
    ```typescript
    // Refresh leaderboard if in leaderboard mode
    if (config.leaderboardMode) {
      await maybeRefreshLeaderboard(bot, env, config, db);
    }
    ```

    **3. Discord notification after recordTrade in processTrader (DISC-04):**

    Add a `buildPortfolioSummary` helper function:
    ```typescript
    async function buildPortfolioSummary(
      db: ReturnType<typeof createDb>,
      botInstanceId: number | undefined,
      cashBalance: number
    ): Promise<TradeNotification["portfolioSummary"]> {
      // Import positions and trades from schema (already imported at top: trackedTraders)
      // Need to also import: positions, trades from schema
      const openPos = botInstanceId
        ? await db.select().from(positions)
            .where(and(eq(positions.botInstanceId, botInstanceId), eq(positions.status, "open")))
        : [];
      const allTrades = botInstanceId
        ? await db.select().from(trades)
            .where(eq(trades.botInstanceId, botInstanceId))
        : [];

      const equity = openPos.reduce((sum, p) => sum + (p.size * (p.currentPrice ?? p.avgEntry)), 0);
      const realizedPnl = allTrades.reduce((sum, t) => sum + (t.pnl ?? 0), 0);
      const totalFees = allTrades.reduce((sum, t) => sum + (t.fee ?? 0), 0);

      return {
        cash: cashBalance,
        equity: Math.round(equity * 100) / 100,
        realizedPnl: Math.round(realizedPnl * 100) / 100,
        totalFees: Math.round(totalFees * 100) / 100,
        netPnl: Math.round((realizedPnl - totalFees) * 100) / 100,
        openPositions: openPos.length,
        totalTrades: allTrades.length,
      };
    }
    ```

    Add imports from schema at top (extend existing import):
    ```typescript
    import { trackedTraders, positions, trades } from "../../core/db/schema";
    ```

    In `processTrader`, after EACH successful `await (bot as any).recordTrade(...)` call (there are two — one for buy copy, one for sell copy), add:
    ```typescript
    // Discord notification (fire-and-forget)
    if (env.DISCORD_WEBHOOK_URL) {
      const cashBalance = await client.getBalance().catch(() => 0);
      const summary = await buildPortfolioSummary(db, config.dbBotId, cashBalance);
      await notifyDiscord(env.DISCORD_WEBHOOK_URL, {
        tradeType: sizeDelta > 0 ? "COPY_BUY" : "COPY_SELL",
        marketName: marketInfo?.title ?? pos.marketId,
        outcome: pos.outcome as "yes" | "no",
        price: orderResult.filledPrice ?? estimatedPrice,
        shares: orderResult.filledSize ?? finalSize,
        cost: (orderResult.filledPrice ?? estimatedPrice) * (orderResult.filledSize ?? finalSize),
        fee: 0, // Fee not directly available from orderResult; set 0 for now
        pnl: undefined, // Buy has no P&L
        traderAddress: traderId,
        timestamp: new Date().toISOString(),
        portfolioSummary: summary,
      });
    }
    ```

    For the sell copy branch, use `tradeType: "COPY_SELL"` and pass sell-specific values (sellPrice, sellSize). Include `pnl: undefined` (would need position history to compute; not available in current flow).

    **4. Thread `env` and `db` to processTrader (already available as params).**

    No new function parameters needed — `env` and `db` are already passed to `processTrader`.
  </action>
  <verify>
    <automated>cd /Users/youanden/Work/trade-bot && grep -c "notifyDiscord\|fetchLeaderboard\|maybeRefreshLeaderboard\|buildPortfolioSummary" src/worker/bots/copy-trader/strategy.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/worker/bots/copy-trader/strategy.ts contains `import { notifyDiscord }` from notifications/discord
    - src/worker/bots/copy-trader/strategy.ts contains `import { fetchLeaderboard }` from polymarket/leaderboard
    - src/worker/bots/copy-trader/strategy.ts contains `if (env.DISCORD_WEBHOOK_URL)` guard before notifyDiscord call
    - src/worker/bots/copy-trader/strategy.ts contains `if (config.leaderboardMode)` guard before maybeRefreshLeaderboard call
    - src/worker/bots/copy-trader/strategy.ts contains `async function maybeRefreshLeaderboard(`
    - src/worker/bots/copy-trader/strategy.ts contains `async function buildPortfolioSummary(`
    - src/worker/bots/copy-trader/strategy.ts contains `config.leaderboardRefreshMs ?? 3_600_000`
    - src/worker/bots/copy-trader/strategy.ts contains `config.leaderboardTopN ?? 10`
    - grep count returns at least 6 (notifyDiscord x2, fetchLeaderboard x1, maybeRefreshLeaderboard x2, buildPortfolioSummary x2)
  </acceptance_criteria>
  <done>copyTraderTick integrates Discord notifications (fire-and-forget after recordTrade when webhook URL configured) and leaderboard refresh (before trader loop when leaderboardMode enabled).</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration tests for Discord and leaderboard in copy-trader</name>
  <files>test/strategies/copy-trader.test.ts</files>
  <read_first>
    - test/strategies/copy-trader.test.ts
    - test/helpers/mocks.ts
    - test/helpers/db.ts
    - src/worker/bots/copy-trader/strategy.ts
    - src/worker/core/db/schema.ts
  </read_first>
  <behavior>
    - Test 1 (DISC-04): When env.DISCORD_WEBHOOK_URL is set and a trade executes, fetch is called with the webhook URL (captured via globalThis.fetch spy)
    - Test 2 (DISC-04): When env.DISCORD_WEBHOOK_URL is absent, fetch is NOT called with any discord-looking URL
    - Test 3 (LEAD-02): When leaderboardMode is true and no _lastLeaderboardRefresh set, strategy fetches leaderboard and inserts traders into tracked_traders table
    - Test 4 (LEAD-03): When leaderboardMode is true and _lastLeaderboardRefresh is recent (< leaderboardRefreshMs), leaderboard fetch is skipped
    - Test 5 (LEAD-02): Leaderboard-sourced traders are stored with lowercase proxyWallet in tracked_traders
  </behavior>
  <action>
    Extend `test/strategies/copy-trader.test.ts` with new describe blocks:

    **Key test setup challenge:** The existing test uses `mock.module` to replace `createDb` and `createExchangeClient`. For Discord notification tests, we also need to intercept the global `fetch` that `notifyDiscord` uses, AND the global `fetch` that `fetchLeaderboard` and `fetchTraderPositions` use. Use `globalThis.fetch` override in beforeEach/afterEach.

    **Add a fetch spy wrapper** at the top of the file (before mock.module calls):
    ```typescript
    let fetchCalls: Array<{ url: string; init?: RequestInit }> = [];
    const origFetch = globalThis.fetch;
    ```

    In beforeEach, add:
    ```typescript
    fetchCalls = [];
    globalThis.fetch = async (input: any, init?: any) => {
      const url = typeof input === "string" ? input : input.url;
      fetchCalls.push({ url, init });
      // Mock leaderboard API
      if (url.includes("data-api.polymarket.com/v1/leaderboard")) {
        return new Response(JSON.stringify([
          { rank: "1", proxyWallet: "0xABC123def456", userName: "TopTrader", pnl: 50000, vol: 0 },
        ]));
      }
      // Mock trader positions API
      if (url.includes("data-api.polymarket.com/positions")) {
        return new Response(JSON.stringify([]));
      }
      // Mock Discord webhook
      if (url.includes("discord.com/api/webhooks")) {
        return new Response(null, { status: 204 });
      }
      return new Response("Not Found", { status: 404 });
    };
    ```

    In afterEach, add:
    ```typescript
    globalThis.fetch = origFetch;
    ```

    **New describe block: "discord notifications"**
    - Test "calls Discord webhook after trade when DISCORD_WEBHOOK_URL set": Create bot with `traderIds: ["0xtrader1"]`, set `env.DISCORD_WEBHOOK_URL = "https://discord.com/api/webhooks/test/token"`. Mock fetchTraderPositions to return a position (via the fetch spy returning positions data). After tick, assert fetchCalls contains an entry with url matching the webhook URL.
    - Test "does not call Discord webhook when DISCORD_WEBHOOK_URL absent": Same setup but no DISCORD_WEBHOOK_URL on env. Assert no fetchCalls to discord.com.

    **New describe block: "leaderboard mode"**
    - Test "refreshes leaderboard and populates tracked_traders": Create bot with `leaderboardMode: true, traderIds: []`. Run tick. Query `trackedTraders` table — expect at least 1 row with `traderId = "0xabc123def456"` (lowercase).
    - Test "skips leaderboard refresh when interval not elapsed": Create bot with `leaderboardMode: true, _lastLeaderboardRefresh: new Date().toISOString(), leaderboardRefreshMs: 3_600_000`. Run tick. Assert no fetch calls to leaderboard URL.
    - Test "stores traders with lowercase address": After leaderboard refresh, verify `trackedTraders` row has `traderId` in all lowercase.

    **Important:** The existing `mock.module` for `createDb` and `createExchangeClient` must remain. The new tests coexist with existing tests. The fetch spy handles external HTTP calls that strategies make directly (positions API, leaderboard API, Discord webhook).
  </action>
  <verify>
    <automated>cd /Users/youanden/Work/trade-bot && bun test test/strategies/copy-trader.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - test/strategies/copy-trader.test.ts contains describe block with "discord" (case-insensitive)
    - test/strategies/copy-trader.test.ts contains describe block with "leaderboard" (case-insensitive)
    - test/strategies/copy-trader.test.ts contains at least 5 new test cases beyond the existing 2
    - test/strategies/copy-trader.test.ts contains `DISCORD_WEBHOOK_URL`
    - test/strategies/copy-trader.test.ts contains `leaderboardMode`
    - test/strategies/copy-trader.test.ts contains `trackedTraders` table query
    - `bun test test/strategies/copy-trader.test.ts` exits 0
  </acceptance_criteria>
  <done>Copy-trader tests verify: Discord webhook called after trade when URL configured, not called when absent; leaderboard refresh inserts traders on first tick, skipped when interval not elapsed; traders stored lowercase.</done>
</task>

</tasks>

<verification>
- `bun test test/strategies/copy-trader.test.ts` passes all tests (old and new)
- `bun test test/core/discord.test.ts test/core/leaderboard.test.ts test/strategies/copy-trader.test.ts` all pass
- `bun test` full suite passes
- `grep "notifyDiscord" src/worker/bots/copy-trader/strategy.ts` confirms Discord integration
- `grep "fetchLeaderboard" src/worker/bots/copy-trader/strategy.ts` confirms leaderboard integration
</verification>

<success_criteria>
1. copyTraderTick calls notifyDiscord after successful recordTrade when env.DISCORD_WEBHOOK_URL is set
2. copyTraderTick refreshes tracked traders from Polymarket leaderboard when leaderboardMode is true and refresh interval has elapsed
3. Leaderboard-sourced traders upserted to tracked_traders table with lowercase addresses
4. All existing copy-trader tests continue to pass
5. Full test suite green
</success_criteria>

<output>
After completion, create `.planning/phases/09-add-discord-trade-notifications-and-leaderboard-copy-strategy/09-03-SUMMARY.md`
</output>
